const mongoose = require("mongoose");
const cinemaSchema= new mongoose.Schema({
        cinemaName: String,
        cinemaAddress: String,
        cinemaTel: String,
        image: String  // 可选
})
mongoose.model("cinemasModel", cinemaSchema, "cinemaes");